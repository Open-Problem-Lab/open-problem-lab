# Action Use Note

A heat exposure output is analytic until it names the action it could change.

Minimum fields:

- decision user
- action timing
- action without the output
- action with the output
- harm if wrong

If those fields are unknown, keep the result as exposure analysis only.
